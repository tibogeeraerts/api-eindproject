version: "3.9"
services:
 api-eindproject-service:
  image: geeraertstibo/api-eindproject:latest
  ports:
    - "8000:8000"
  volumes:
    - sqlite_api-eindproject_volume:/code/sqlitedb

volumes:
  sqlite_api-eindproject_volume: